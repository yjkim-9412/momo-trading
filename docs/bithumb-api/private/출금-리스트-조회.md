# 출금-리스트-조회

**Method:** GET
**URL:** `https://api.bithumb.com/v1/withdraws`
**인증:** Bearer JWT 토큰

## 파라미터

| 이름 | 타입 | 필수 | 설명 |
|------|------|------|------|
| currency | string | 선택 | 화폐를 의미하는 영문 대문자 코드 |
| state | string | 선택 | 출금 상태 |
| uuids | array[string] | 선택 | 출금 UUID의 목록 |
| txids | array[string] | 선택 | 출금 TXID의 목록 |
| page | integer | 선택 | 페이지 수 (기본값: 1) |
| limit | integer | 선택 | 개수 제한 (기본값: 100) |
| order_by | string | 선택 | 정렬방식 (기본값: "desc") |

## 응답

```json
[
  {
    "type": "string",
    "uuid": "string",
    "currency": "string",
    "net_type": null,
    "txid": "string",
    "state": "string",
    "created_at": "string",
    "done_at": "string",
    "amount": "string",
    "fee": "string",
    "transaction_type": null
  }
]
```

| 필드 | 타입 | 설명 |
|------|------|------|
| type | string | 거래 타입 (예: "withdraw") |
| uuid | string | 출금 UUID |
| currency | string | 화폐 코드 |
| net_type | null | 네트워크 타입 |
| txid | string | 거래 ID |
| state | string | 출금 상태 |
| created_at | string | 생성 시간 (ISO 8601) |
| done_at | string | 완료 시간 (ISO 8601) |
| amount | string | 출금액 |
| fee | string | 수수료 |
| transaction_type | null | 거래 분류 |

## 인증

**방식:** JWT (JSON Web Token)

**필수 클레임:**
- `access_key` - API 액세스 키
- `nonce` - 고유 식별자 (UUID v4 권장)
- `timestamp` - 현재 Unix 타임스탬프 (밀리초)
- `query_hash` - 쿼리 문자열의 SHA-512 해시
- `query_hash_alg` - "SHA512"

## 코드 예제

### Python
```python
import jwt
import uuid
import hashlib
import time
import requests

accessKey = 'YOUR_API_KEY'
secretKey = 'YOUR_SECRET_KEY'
apiUrl = 'https://api.bithumb.com'

param = dict(currency='XRP', limit=100, page=1, order_by='desc')
query = '&'.join([f'{k}={v}' for k, v in param.items()])
hash_obj = hashlib.sha512()
hash_obj.update(query.encode('utf-8'))
query_hash = hash_obj.hexdigest()

payload = {
  'access_key': accessKey,
  'nonce': str(uuid.uuid4()),
  'timestamp': round(time.time() * 1000),
  'query_hash': query_hash,
  'query_hash_alg': 'SHA512'
}
jwt_token = jwt.encode(payload, secretKey)
headers = { 'Authorization': f'Bearer {jwt_token}' }

response = requests.get(apiUrl + '/v1/withdraws?' + query, headers=headers)
print(response.status_code)
print(response.json())
```

### JavaScript
```javascript
const jwt = require('jsonwebtoken');
const { v4: uuidv4 } = require('uuid');
const crypto = require('crypto');
const axios = require('axios');

const accessKey = 'YOUR_API_KEY';
const secretKey = 'YOUR_SECRET_KEY';
const apiUrl = 'https://api.bithumb.com';

var query = 'currency=XRP&limit=100&page=1&order_by=desc';
const uuids = ['200268229', '200268132'];
const uuid_query = uuids.map(uuid => `uuids[]=${uuid}`).join('&');
query = uuid_query ? query + "&" + uuid_query : query;

const alg = 'SHA512';
const hash = crypto.createHash(alg);
const queryHash = hash.update(query, 'utf-8').digest('hex');
const payload = {
  access_key: accessKey,
  nonce: uuidv4(),
  timestamp: Date.now(),
  query_hash: queryHash,
  query_hash_alg: alg
};
const jwtToken = jwt.sign(payload, secretKey);
const config = {
  headers: { Authorization: `Bearer ${jwtToken}` }
};

axios.get(apiUrl + '/v1/withdraws?' + query, config)
  .then(response => console.log(response.data))
  .catch(error => console.error(error.response.data));
```

### Java
```java
import com.auth0.jwt.JWT;
import com.auth0.jwt.algorithms.Algorithm;
import org.apache.http.client.methods.CloseableHttpResponse;
import org.apache.http.client.methods.HttpGet;
import org.apache.http.impl.client.CloseableHttpClient;
import org.apache.http.impl.client.HttpClients;
import org.apache.http.util.EntityUtils;
import java.math.BigInteger;
import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;
import java.util.UUID;

String accessKey = "YOUR_API_KEY";
String secretKey = "YOUR_SECRET_KEY";
String apiUrl = "https://api.bithumb.com";
String query = "currency=XRP&limit=100&page=1&order_by=desc";

MessageDigest md = MessageDigest.getInstance("SHA-512");
md.update(query.getBytes(StandardCharsets.UTF_8));
String queryHash = String.format("%0128x", new BigInteger(1, md.digest()));

Algorithm algorithm = Algorithm.HMAC256(secretKey);
String jwtToken = JWT.create()
  .withClaim("access_key", accessKey)
  .withClaim("nonce", UUID.randomUUID().toString())
  .withClaim("timestamp", System.currentTimeMillis())
  .withClaim("query_hash", queryHash)
  .withClaim("query_hash_alg", "SHA512")
  .sign(algorithm);

HttpGet httpRequest = new HttpGet(apiUrl + "/v1/withdraws?" + query);
httpRequest.addHeader("Authorization", "Bearer " + jwtToken);

try (CloseableHttpClient client = HttpClients.createDefault();
     CloseableHttpResponse response = client.execute(httpRequest)) {
  String responseBody = EntityUtils.toString(response.getEntity());
  System.out.println(response.getStatusLine().getStatusCode());
  System.out.println(responseBody);
}
```
